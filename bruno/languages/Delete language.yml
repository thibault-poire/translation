info:
  name: Delete language
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{API_URL}}/languages/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
