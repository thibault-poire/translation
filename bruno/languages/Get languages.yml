info:
  name: Get languages
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/languages"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
