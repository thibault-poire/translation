info:
  name: Delete project
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{API_URL}}/projects/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
