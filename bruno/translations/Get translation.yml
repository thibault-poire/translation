info:
  name: Get translation
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/translations/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
