info:
  name: Get user
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/users/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
