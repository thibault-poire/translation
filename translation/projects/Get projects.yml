info:
  name: Get projects
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
