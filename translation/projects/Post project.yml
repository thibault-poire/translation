info:
  name: Post project
  type: http
  seq: 3

http:
  method: POST
  url: "{{API_URL}}/projects"
  body:
    type: form-urlencoded
    data:
      - name: name
        value: Test
      - name: default_language_tag
        value: fr-FR
      - name: team_ids
        value: "[]"
        disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
